/**
 * @type {import("../../../types.d.ts").HandlerInfo}
 */
const info = {
	path: "/discord/leaderboard/",
	handlers: {
		get: async (logger, settings, req, res) => {

		}
	}
};

module.exports = info;